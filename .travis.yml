language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-oauth-api-server' ]; then cd .. && eval "mv $currentfolder generator-oauth-api-server" && cd generator-oauth-api-server; fi

